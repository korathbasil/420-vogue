import { SearchHeader } from "components";

const SearchPage = () => {
  return (
    <main>
      <SearchHeader query="hh" setQuery={(s: string) => {}} />
    </main>
  );
};

export default SearchPage;
